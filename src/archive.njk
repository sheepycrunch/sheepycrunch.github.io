---
layout: layout.njk
title: "{{ i18n_kr.archive.title | t(lang) }}"
description: "{{ i18n_kr.archive.description | t(lang) }}"
lang: "kr"
---

<section>
  <h1>{{ i18n_kr.archive.title | t(lang) }}</h1>
  
  <div class="archive-list">
    {% for item in posts.posts %}
      {% if item.category == "archive" %}
      <div class="archive-item">
        <div class="archive-thumbnail">
          {% if item.image %}
          <img src="{{ item.image }}" alt="{{ item.description }}" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
          {% endif %}
          <div class="thumbnail-placeholder" {% if item.image %}style="display: none;"{% endif %}>
          </div>
        </div>
        <div class="archive-content">
          <h3><a href="{{ item.description }}" class="archive-title-link" target="_blank">{{ item.description }}</a></h3>
          <div class="archive-tags">
            {% for tag in item.tags %}
            <button class="tag-button" onclick="searchByTag('{{ tag }}')">#{{ tag }}</button>
            {% endfor %}
          </div>
        </div>
      </div>
      {% endif %}
    {% endfor %}
  </div>
</section>

