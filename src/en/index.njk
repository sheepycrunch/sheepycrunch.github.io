---
layout: layout.njk
title: "DIY Hamster Dungeon"
description: "DIY Hamster Dungeon - Personal Blog"
showNavigation: true
css: "/index.css"
lang: "en"
---

<!-- 아이콘 쇼케이스 섹션 -->
<section id="icon-showcase">
  <ol>
    <li><img src="/images/icons/icon1.png" alt="icon" onerror="this.style.display='none'"></li>
    <li class="hidden"><img src="/images/icons/icon2.png" alt="icon" onerror="this.style.display='none'"></li>
    <li class="hidden"><img src="/images/icons/icon3.png" alt="icon" onerror="this.style.display='none'"></li>
    <li class="hidden"><img src="/images/icons/icon4.png" alt="icon" onerror="this.style.display='none'"></li>
    <li class="hidden"><img src="/images/icons/icon5.png" alt="icon" onerror="this.style.display='none'"></li>
  </ol>
  <img id="misuzu-button" class="clickable" src="/images/button.png" alt="click to change icon" onerror="this.style.display='none'">
</section>

<!-- 자기소개 섹션 -->
<section>
  <h2>{{ i18n_en.sections.selfIntroduction }}</h2>
  <dl id="name-display">
    <dt>{{ i18n_en.author.name }}</dt>
    <dd>{{ site.author.name }}</dd>
  </dl>
  <p>{{ i18n_en.author.introduction }}</p>
  <p>{{ i18n_en.author.hobbies }}: {{ i18n_en.hobbies | join(', ') }}</p>
  <p>{{ i18n_en.author.currentProject }}</p>
</section>

<!-- 사이트 소개 섹션 -->
<section>
  <h2>{{ i18n_en.sections.aboutSite }}</h2>
  <p>{{ i18n_en.aboutSite.text1 }}</p>
  <p>{{ i18n_en.aboutSite.text2 }}</p>
</section>

<!-- SNS 링크 섹션 -->
<section>
  <h2>{{ i18n_en.sections.snsLinks }}</h2>
  <ul class="horizontal-layout">
    {% if site.social.twitter %}
    <li>
      <a href="{{ site.social.twitter }}" target="_blank">
        <img src="/images/social/twitter.png" alt="Twitter" class="link-icon clickable" onerror="this.style.display='none'">
      </a>
    </li>
    {% endif %}
    {% if site.social.github %}
    <li>
      <a href="{{ site.social.github }}" target="_blank">
        <img src="/images/social/github.png" alt="GitHub" class="link-icon clickable" onerror="this.style.display='none'">
      </a>
    </li>
    {% endif %}
    {% if site.social.pixiv %}
    <li>
      <a href="{{ site.social.pixiv }}" target="_blank">
        <img src="/images/social/pixiv.png" alt="Pixiv" class="link-icon clickable" onerror="this.style.display='none'">
      </a>
    </li>
    {% endif %}
  </ul>
</section>

<!-- 콘텐츠 섹션 -->
<section>
  <h2>{{ i18n_en.sections.content }}</h2>
  <ul class="horizontal-layout">
    {% for nav in i18n_en.navigation %}
    <li>
      <a href="{{ nav.url | localizedUrl('en') }}">
        <div class="clickable button-like-decoration">
          <p>{{ nav.name }}</p>
        </div>
      </a>
    </li>
    {% endfor %}
  </ul>
</section>

<!-- 통계 정보 섹션 -->
<section>
  <h2>{{ i18n_en.sections.siteInfo }}</h2>
  <div class="info-stats">
    <div class="stat-item">
      <span class="stat-label">{{ i18n_en.stats.posts }}:</span>
      <span class="stat-value">{{ site.stats.posts }}</span>
    </div>
    <div class="stat-item">
      <span class="stat-label">{{ i18n_en.stats.visitors }}:</span>
      <span class="stat-value">{{ site.stats.visitors }}</span>
    </div>
    <div class="stat-item">
      <span class="stat-label">{{ i18n_en.stats.lastUpdate }}:</span>
      <span class="stat-value">{{ site.stats.lastUpdate }}</span>
    </div>
  </div>
</section>

<!-- 최신 업데이트 섹션 -->
<section>
  <h2>{{ i18n_en.sections.latestUpdates }}</h2>
    <div class="update-list">
      {% for update in i18n_en.updates %}
      <div class="update-item">
        <span class="update-date">{{ update.date }}</span>
        <span class="update-text">{{ update.text }}</span>
      </div>
      {% endfor %}
    </div>
</section>

<!-- 기능 소개 섹션 -->
<section>
  <h2>{{ i18n_en.sections.mainFeatures }}</h2>
    <div class="feature-list">
    {% for feature in i18n_en.features %}
      <div class="feature-item">
      <span class="feature-icon">{{ feature.icon }}</span>
      <span class="feature-title">{{ feature.title }}</span>
      <span class="feature-desc">{{ feature.description }}</span>
    </div>
    {% endfor %}
  </div>
</section>

<!-- 방문자 메시지 -->
<section class="visitor-message">
  <h3>{{ i18n_en.visitorMessage.title }}</h3>
  <p>{{ i18n_en.visitorMessage.text }}</p>
  <div class="message-actions">
    <a href="/contact" class="button">{{ i18n_en.visitorMessage.actions.sendMessage }}</a>
    <a href="/gallery" class="button secondary">{{ i18n_en.visitorMessage.actions.viewGallery }}</a>
  </div>
</section>

<!-- JavaScript 파일들 -->
<script src="/js/icon-changer.js"></script>
<script src="/js/stats-manager.js"></script>
<script src="/js/admin-panel.js"></script>
