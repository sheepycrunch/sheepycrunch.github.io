name: Upload Images to Neocities and Nekoweb

on:
  push:
    paths:
      - 'src/images/**'
      - 'images/**'
  workflow_dispatch: # 수동 실행 가능

jobs:
  upload-images:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      
    - name: Upload images to Neocities
      uses: bcomnes/deploy-to-neocities@v1
      with:
        api_token: ${{ secrets.NEOCITIES_API_KEY }}
        cleanup: false # 기존 파일 유지
        dist_dir: src/images # 이미지 폴더만 업로드
        
    - name: Upload images to Nekoweb
      uses: ./.github/actions/deploy-nekoweb
      with:
        api_key: ${{ secrets.NEKOWEB_API_KEY }}
        site_name: ${{ secrets.SITE_NAME }}
        dist_dir: src/images # 이미지 폴더만 업로드
        cleanup: false
